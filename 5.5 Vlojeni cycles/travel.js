function travel(input){
    let index = 0;
    let n = input[index];
    index++;
    let price = input[index];
    index++;
    let neededMoney = price;
    let savings = 0;
    while(input[index] !== End){
        if(input[index] === text[input(index)]){
            
        }
    }
}